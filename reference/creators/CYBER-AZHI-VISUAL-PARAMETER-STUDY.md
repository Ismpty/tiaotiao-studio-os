# CYBER-AZHI-VISUAL-PARAMETER-STUDY — 赛博阿智pro AIGC Basic Visual Parameter Reference

> Reference Study（参考研究）
>
> Status：Source Link Resolved / Page Content Blocked / Canonical Knowledge Promoted
>
> Canonical Output：`knowledge/visual-parameters/VISUALPARAM-001.md`

---

# Purpose（用途）

This file records the source capture for a Douyin note by `赛博阿智pro` about AIGC basic visual parameters for AI short drama production.

It is a reference record, not a model-facing prompt library.

Reusable TSOS rules from this study have been converted into:

```text
knowledge/visual-parameters/VISUALPARAM-001.md
```

---

# Source（来源）

| Field | Value |
|---|---|
| Platform | Douyin |
| Creator | 赛博阿智pro |
| User-provided share URL | `https://v.douyin.com/L5OdepTLrX4/` |
| Resolved note ID | `7657167225338987919` |
| Resolved canonical URL | `https://www.douyin.com/note/7657167225338987919` |
| Source Type | Image-text note |
| User-provided visible title | `全网最全AIGC基础画面参数。AI短剧吃透这些镜头...` |
| User-provided visible date token | `05/24 :3pm` |

---

# Capture Result（采集结果）

The short link resolved successfully to Douyin note ID:

```text
7657167225338987919
```

The PC canonical page returned `404` during capture.

The `iesdouyin` share page returned an anti-crawler JavaScript challenge rather than SSR note metadata or image resources.

Therefore, TSOS captured:

```text
[x] Share URL
[x] Note ID
[x] Creator name from user-provided share text
[x] Visible title from user-provided share text
[x] Source theme: AIGC basic visual parameters for AI short drama
```

TSOS did not capture:

```text
[ ] Full note body
[ ] Slide images
[ ] Image URI list
[ ] OCR text
[ ] Exact original parameter list
```

---

# Extraction Boundary（提取边界）

Because the page content was blocked, TSOS must not claim that it has fully extracted the original note.

This study only supports a source-inspired canonical TSOS node:

```text
knowledge/visual-parameters/VISUALPARAM-001.md
```

The canonical node is a TSOS-authored visual parameter grammar based on:

- the verified source theme
- existing TSOS cinematic documentary rules
- existing TSOS camera, color, transition and prompt runtime architecture

It is not a copied transcription of the Douyin note.

---

# TSOS Conversion（TSOS 转写）

TSOS converted the source theme into a reusable visual parameter grammar covering:

```text
1. Format / Canvas
2. Shot Size
3. Camera Angle / Height
4. Lens / Focal Length
5. Composition
6. Depth of Field
7. Lighting
8. Color / Tone
9. Texture / Detail
10. Motion / Temporal Parameters
11. Continuity Parameters
12. Negative Constraints
```

This makes future video prompts easier to review and repeat.

---

# Use Rule（使用规则）

When creating future prompts, do not paste source text directly.

Use the canonical TSOS node instead:

```text
knowledge/visual-parameters/VISUALPARAM-001.md
```

Model-facing prompts must describe visual parameters in natural language and must not depend on raw internal IDs.

---

# Next Verification Step（下一步验证）

If full fidelity extraction is required later:

```text
1. Re-open the Douyin note in a logged-in browser or app.
2. Capture screenshots of all slides.
3. Run OCR or manual transcription.
4. Compare extracted parameters against VISUALPARAM-001.
5. Promote only reusable TSOS-compatible rules, not copied wording.
```
