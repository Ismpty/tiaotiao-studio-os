# YIFAN-OVERHEAD-BLOCKING-STUDY — AI Overhead Scene Blocking Map Reference

> Reference Study（参考研究）
>
> Status：Source Link Resolved / Page Content Blocked / Canonical Knowledge Promoted
>
> Canonical Output：`knowledge/scene-blocking/BLOCKING-001.md`

---

# Purpose（用途）

This file records the source capture for a Douyin video reference about using one overhead scene blocking map to reduce ineffective AI video generation attempts.

It is a reference record, not a model-facing prompt library.

Reusable TSOS rules from this study have been converted into:

```text
knowledge/scene-blocking/BLOCKING-001.md
```

---

# Source（来源）

| Field | Value |
|---|---|
| Platform | Douyin |
| Attributed Teacher | 一帆老师 |
| User-provided share URL | `https://v.douyin.com/uE5U0gh3o8s/` |
| Resolved video ID | `7658676046082909425` |
| Resolved canonical URL | `https://www.douyin.com/video/7658676046082909425` |
| Source Type | Video |
| User-provided visible date token | `01/12 :4pm` |
| User-provided tags | `#AI视频` `#AI影视` `#ai新星计划` `#AI创作浪潮计划` `#AI短剧` |

---

# User-Provided Source Summary（用户提供摘要）

The user-provided text describes the problem:

```text
AI video generation often fails when scenes involve multi-person dialogue, chasing, fighting or camera switching. Character positions drift and spatial relationships collapse.
```

The described method:

```text
Use one AI overhead scene blocking map to plan complex positioning, movement, fighting, dialogue and one-take camera movement before video generation.
```

---

# Capture Result（采集结果）

The short link resolved successfully to Douyin video ID:

```text
7658676046082909425
```

The PC canonical page returned `404` during capture.

The `iesdouyin` share page returned an anti-crawler JavaScript challenge rather than SSR video metadata.

Therefore, TSOS captured:

```text
[x] Share URL
[x] Video ID
[x] Source theme from user-provided text
[x] Use cases: multi-person dialogue, chase, fight blocking, one-take camera movement
[x] Canonical TSOS scene-blocking grammar created
```

TSOS did not capture:

```text
[ ] Full video transcript
[ ] Video frames
[ ] Original diagrams
[ ] Exact original prompt wording
```

---

# Extraction Boundary（提取边界）

Because the page content was blocked, TSOS must not claim that it fully watched or transcribed the video.

This study supports a source-inspired canonical TSOS node:

```text
knowledge/scene-blocking/BLOCKING-001.md
```

The canonical node is a TSOS-authored scene-blocking grammar based on:

- the user-provided source summary
- the verified resolved video ID
- existing TSOS storyboard and prompt runtime rules
- existing TSOS camera, visual parameter, transition and continuity rules

It is not a copied transcript of the Douyin video.

---

# TSOS Conversion（TSOS 转写）

TSOS converted the source theme into a reusable overhead blocking system covering:

```text
1. Scene boundary
2. Entrances and exits
3. Key props and obstacles
4. Character markers
5. Starting positions
6. Movement paths
7. Camera positions
8. Camera movement paths
9. Shot-change points
10. Interaction zones
11. Continuity locks
```

This makes future video prompts easier to stabilize before generation.

---

# Use Rule（使用规则）

When creating future prompts, do not paste source text directly.

Use the canonical TSOS node instead:

```text
knowledge/scene-blocking/BLOCKING-001.md
```

Model-facing prompts must describe the overhead blocking map in natural language and must not depend on raw internal IDs.

---

# Next Verification Step（下一步验证）

If full fidelity extraction is required later:

```text
1. Re-open the Douyin video in a logged-in browser or app.
2. Capture the video frames that show the overhead map method.
3. Transcribe spoken / captioned guidance.
4. Compare extracted details against BLOCKING-001.
5. Promote only reusable TSOS-compatible rules, not copied wording.
```
